# Cross-Verified Proxy Server Environment Variables

# Server Configuration
NODE_ENV=production
PORT=3000

# CORS Configuration
ALLOWED_ORIGINS=https://cross-verified-ai.app,https://cross-verified-ai.onrender.com

# Database Configuration (PostgreSQL 17)
DATABASE_URL=postgresql://user:password@host:5432/dbname
DATABASE_URL_INTERNAL=postgresql://user:password@internal-host:5432/dbname
DATABASE_URL_EXTERNAL=postgresql://user:password@external-host:5432/dbname

# Security Keys (Generate using crypto.randomBytes(32).toString('hex'))
ENCRYPTION_KEY=your-32-byte-encryption-key-in-hex-format-64-chars
JWT_SECRET=your-jwt-secret-key-here
SESSION_SECRET=your-session-secret-key-here
HMAC_SECRET=your-hmac-secret-key-here

# Google OAuth 2.0
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_ORIGIN=https://cross-verified-ai.onrender.com
GOOGLE_REDIRECT_URI=https://cross-verified-ai.onrender.com/auth/google/callback

# Notes:
# - ENCRYPTION_KEY must be exactly 64 hex characters (32 bytes)
# - All *_SECRET variables should be generated using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# - DATABASE_URL_INTERNAL is for Render internal connections
# - DATABASE_URL_EXTERNAL is for external/development connections
# - ALLOWED_ORIGINS should be comma-separated list of allowed domains (no wildcards '*' in production)
