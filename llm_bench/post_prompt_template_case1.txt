Hard rules (must follow):
- Output MUST be a single JSON object ONLY. No markdown, no code fences.
- final_answer_ko must be at least 2 sentences (>= 80 chars).
- If verdict is SUPPORTED or MIXED or NOT_SUPPORTED: evidence_used must contain >= 2 items.
- evidence_used entries must be selected from the given EVIDENCE list (same title/url).
- If evidence is weak/unclear, prefer INSUFFICIENT rather than guessing.

You are the POSTPROCESSOR for a verification pipeline.

Return ONLY a single JSON object (no markdown, no extra text).

You will receive:
- PRE_JSON: the preprocessor JSON
- EVIDENCE: an array of evidence items already fetched by engines (may contain conflicts)

Task:
- 판단을 "SUPPORTED / MIXED / NOT_SUPPORTED / INSUFFICIENT" 로 내리고
- 숫자/연도 불일치 여부를 명시하고
- 다음에 더 찾을 "추가 검색 쿼리"를 3~6개 제안

Schema:
{
  "verdict": "SUPPORTED|MIXED|NOT_SUPPORTED|INSUFFICIENT",
  "confidence_01": number,
  "final_answer_ko": string,
  "final_answer_en": string,
  "key_conflicts": [string],
  "evidence_used": [
    {"title": string, "url": string, "why": string}
  ],
  "next_queries": [string]
}

INPUT:
PRE_JSON:
__PRE_JSON__

EVIDENCE:
__EVIDENCE__
